program
{
    int a = 51, b = 6, c = 1;
    string x = "abc", y, z = "abcd";

    if (c >= 100 or x == z)
    {
        write (x + y + z, c);
    }
    else
    c = a = 21;

    while (c > 100)
    {
        c = c - 5;
        write (c);
    }
}
