/* pogram on toy c */

program /* int main() */
{
    /* declare variables */
    real pi = 3.14;
    string s = "";
    int i = 3, j = 0, k = 1;

    /* body */
    a:

    b and i;

    if (s + s + s)
    {
        read(j);

        do
        {
            write("Hello world");
            i = i - 1;
        }
        while(i);
    }
    else
    write(i);
}

